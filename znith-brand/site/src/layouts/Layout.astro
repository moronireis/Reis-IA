---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  currentPath?: string;
}

const { title, description = 'ZNITH Brandbook & Design System — Versão 1.0', currentPath = '/' } = Astro.props;

const navGroups = [
  {
    label: 'MARCA',
    items: [
      { href: '/marca', label: 'A Marca' },
      { href: '/arquitetura', label: 'Arquitetura' },
    ],
  },
  {
    label: 'IDENTIDADE',
    items: [
      { href: '/identidade', label: 'Visual' },
      { href: '/cores', label: 'Cores' },
      { href: '/tipografia', label: 'Tipografia' },
      { href: '/grafismos', label: 'Grafismos' },
    ],
  },
  {
    label: 'SISTEMA',
    items: [
      { href: '/componentes', label: 'Componentes' },
    ],
  },
  {
    label: 'MESSAGING',
    items: [
      { href: '/voz', label: 'Tom de Voz' },
    ],
  },
  {
    label: 'CAMADAS',
    items: [
      { href: '/personal', label: 'Leilaine Campioto' },
      { href: '/produto', label: 'ZNITH.AI OS' },
      { href: '/movimento', label: 'Líderes Leões' },
    ],
  },
  {
    label: 'APLICAÇÃO',
    items: [
      { href: '/aplicacoes', label: 'Aplicações' },
    ],
  },
];
---

<!doctype html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{title} — ZNITH Brandbook</title>
    <link rel="icon" type="image/png" href="/logos/Z-3.png" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;700&family=Montserrat:wght@300;400;500;600;700;800&display=swap" rel="stylesheet" />
  </head>
  <body>
    <!-- Mobile header -->
    <header class="mobile-header" style="display: none;">
      <a href="/" style="display:flex;align-items:center;gap:12px;">
        <img src="/logos/Z-3.png" alt="ZNITH" style="height:36px;width:36px;object-fit:contain;" />
        <span style="font-family:'Cinzel',serif;font-size:20px;color:#FFFFFF;letter-spacing:0.08em;">ZNITH</span>
      </a>
      <button id="menuBtn" aria-label="Menu" style="background:none;border:1px solid #2A3A5A;border-radius:6px;padding:8px 10px;cursor:pointer;color:#BBBBBB;">
        <svg width="18" height="14" viewBox="0 0 18 14" fill="none" xmlns="http://www.w3.org/2000/svg">
          <rect width="18" height="2" fill="currentColor"/>
          <rect y="6" width="18" height="2" fill="currentColor"/>
          <rect y="12" width="18" height="2" fill="currentColor"/>
        </svg>
      </button>
    </header>

    <!-- Sidebar overlay for mobile -->
    <div id="sidebarOverlay" style="display:none;position:fixed;inset:0;background:rgba(5,11,23,0.8);z-index:299;"></div>

    <!-- Sidebar -->
    <nav id="sidebar" style="
      position: fixed;
      left: 0;
      top: 0;
      bottom: 0;
      width: 280px;
      background: #091022;
      border-right: 1px solid #2A3A5A;
      overflow-y: auto;
      z-index: 300;
      display: flex;
      flex-direction: column;
    ">
      <!-- Scroll Progress Bar -->
      <div class="scroll-progress-bar">
        <div class="scroll-progress-fill" id="scrollProgressFill"></div>
      </div>

      <!-- Logo area -->
      <div style="padding:24px 20px 20px;position:relative;">
        <a href="/" style="display:flex;align-items:center;gap:12px;text-decoration:none;">
          <img src="/logos/Z-3.png" alt="ZNITH" style="height:40px;width:40px;object-fit:contain;" />
          <div>
            <div style="font-family:'Cinzel',serif;font-size:18px;color:#FFFFFF;letter-spacing:0.1em;line-height:1;">ZNITH</div>
            <div style="font-family:'Montserrat',sans-serif;font-size:10px;color:#D4A04A;letter-spacing:0.15em;text-transform:uppercase;margin-top:3px;">Brandbook v1.0</div>
          </div>
        </a>
        <!-- Gradient border -->
        <div style="position:absolute;bottom:0;left:20px;right:20px;height:1px;background:linear-gradient(90deg, transparent, rgba(212, 160, 74, 0.15), transparent);"></div>
      </div>

      <!-- Navigation -->
      <div style="padding:16px 0;flex:1;">
        {navGroups.map((group) => (
          <div style="margin-bottom:8px;">
            <div style="
              padding: 6px 20px;
              font-family: 'Montserrat', sans-serif;
              font-size: 10px;
              font-weight: 600;
              text-transform: uppercase;
              letter-spacing: 0.12em;
              color: #3A5080;
              margin-top: 8px;
            ">{group.label}</div>
            {group.items.map((item) => {
              const isActive = currentPath === item.href || (currentPath !== '/' && item.href !== '/' && currentPath.startsWith(item.href));
              return (
                <a
                  href={item.href}
                  class:list={['nav-link', { active: isActive }]}
                >
                  {item.label}
                </a>
              );
            })}
          </div>
        ))}
      </div>

      <!-- Footer -->
      <div style="padding:16px 20px;position:relative;">
        <div style="position:absolute;top:0;left:20px;right:20px;height:1px;background:linear-gradient(90deg, transparent, rgba(212, 160, 74, 0.15), transparent);"></div>
        <div class="sidebar-tagline" style="font-family:'Cinzel',serif;font-size:11px;color:#3A5080;letter-spacing:0.12em;text-align:center;">
          DIREÇÃO. CONSCIÊNCIA. LEGADO.
        </div>
      </div>
    </nav>

    <!-- Main content -->
    <main style="
      margin-left: 280px;
      min-height: 100vh;
      background: #091022;
    ">
      <div style="
        max-width: 900px;
        padding: 0 48px 96px;
        margin: 0 auto;
      " class="main-content">
        <slot />
      </div>
    </main>

    <style>
      @media (max-width: 767px) {
        .mobile-header {
          display: flex !important;
          align-items: center;
          justify-content: space-between;
          padding: 16px 20px;
          background: #091022;
          border-bottom: 1px solid #2A3A5A;
          position: sticky;
          top: 0;
          z-index: 200;
        }
        #sidebar {
          transform: translateX(-100%);
          transition: transform 0.3s ease;
        }
        #sidebar.open {
          transform: translateX(0);
        }
        main {
          margin-left: 0 !important;
        }
        .main-content {
          padding: 0 20px 64px !important;
        }
      }
    </style>

    <script>
      const menuBtn = document.getElementById('menuBtn');
      const sidebar = document.getElementById('sidebar');
      const overlay = document.getElementById('sidebarOverlay');

      function openMenu() {
        sidebar?.classList.add('open');
        if (overlay) overlay.style.display = 'block';
      }

      function closeMenu() {
        sidebar?.classList.remove('open');
        if (overlay) overlay.style.display = 'none';
      }

      menuBtn?.addEventListener('click', openMenu);
      overlay?.addEventListener('click', closeMenu);

      // Close on nav link click (mobile)
      sidebar?.querySelectorAll('a').forEach(link => {
        link.addEventListener('click', closeMenu);
      });

      // Scroll Progress Bar
      const fill = document.getElementById('scrollProgressFill');
      if (fill) {
        const updateProgress = () => {
          const scrollTop = document.documentElement.scrollTop || document.body.scrollTop;
          const scrollHeight = document.documentElement.scrollHeight - document.documentElement.clientHeight;
          const progress = scrollHeight > 0 ? (scrollTop / scrollHeight) * 100 : 0;
          fill.style.width = progress + '%';
        };

        let ticking = false;
        window.addEventListener('scroll', () => {
          if (!ticking) {
            requestAnimationFrame(() => {
              updateProgress();
              ticking = false;
            });
            ticking = true;
          }
        });
        updateProgress();
      }

      // Scroll Reveal Observer
      const revealElements = document.querySelectorAll('[data-reveal], [data-reveal-stagger]');
      if (revealElements.length > 0) {
        const observer = new IntersectionObserver((entries) => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              entry.target.classList.add('revealed');
              observer.unobserve(entry.target);
            }
          });
        }, { threshold: 0.15 });

        revealElements.forEach(el => observer.observe(el));
      }

      // Gold line reveal
      const goldLines = document.querySelectorAll('.gold-line');
      if (goldLines.length > 0) {
        const lineObserver = new IntersectionObserver((entries) => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              entry.target.classList.add('revealed');
              lineObserver.unobserve(entry.target);
            }
          });
        }, { threshold: 0.5 });

        goldLines.forEach(el => lineObserver.observe(el));
      }
    </script>
  </body>
</html>
