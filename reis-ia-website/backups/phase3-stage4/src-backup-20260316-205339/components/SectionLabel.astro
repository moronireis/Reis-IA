---
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---

<p
  class={`text-[12px] font-semibold uppercase ${className}`}
  style="color: rgba(255,255,255,0.4); letter-spacing: 0.10em;"
>
  <slot />
</p>
