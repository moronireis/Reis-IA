---
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---

<p class={`text-[13px] font-semibold uppercase tracking-widest text-neutral-500 ${className}`}>
  <slot />
</p>
