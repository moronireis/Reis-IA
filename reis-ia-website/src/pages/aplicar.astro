---
/**
 * /aplicar -- Redirect to /agendar?source=builders
 * The Builders application flow now runs through the unified typebot.
 * Source param triggers the Builders-specific intro message and lead source tag.
 */
export const prerender = false;

return Astro.redirect('/agendar?source=builders', 302);
---
