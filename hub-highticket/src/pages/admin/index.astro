---
import AdminLayout from '../../layouts/AdminLayout.astro';
import ExpertDashboardPage from '../../components/dashboard/ExpertDashboardPage';

const user = Astro.locals.user;
---

<AdminLayout title="Hub HT — Dashboard" breadcrumbs={[{ label: 'Dashboard' }]}>
  <ExpertDashboardPage client:load userName={user?.name?.split(' ')[0] || 'Expert'} />
</AdminLayout>
