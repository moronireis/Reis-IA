---
const session = Astro.locals.session;
const user = Astro.locals.user;

if (session && user) {
  return Astro.redirect(user.role === 'student' ? '/aluno' : '/admin');
}

return Astro.redirect('/login');
---
